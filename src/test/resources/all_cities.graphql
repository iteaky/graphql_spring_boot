query GET_ALL_CITIES {
    cities{
        id,
        name,
        country,
        streets {
            id,
            name,
            area,
            owner,
            malls {
                id,
                name,
                building
            }
        }
    }
}